{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${display_name}",
  "description": "${description}",
  "authors": [
    "${authors}"
  ],
  "contributors": [
    "Name suggested by Moritz Walter"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "${issues}",
    "sources": "${source}"
  },
  "license": "${license}",
  "icon": "${icon}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${maven_group}.fabric.${main_class_name}Fabric"
    ],
    "client": [
      "${maven_group}.fabric.client.ClientSetup"
    ],
    "fabric-datagen": [
      "${maven_group}.fabric.DataGenEntry"
    ]
  },
  "mixins": [
    "${modid}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=${minecraft_version} <${next_unsupported_minecraft_version}",
    "java": ">=${java_version}",
    "dragonlib": ">=${dragonlib_version}",
    "geckolib": ">=${geckolib_fabric_version}",
    "create": "*",
    "fabric-api": "*"
  },
  "custom": {
    "modmenu": {
      "links": {
        "Discord": "${discord}"
      },
      "update_checker": true
    }
  },
  "accessWidener": "${modid}.accesswidener"
}
